import React from 'react';
import { View, Text } from 'react-native';

const Home = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor:"#88B04B"}}>
            <Text style={{color:"white", fontSize:30}}>Home</Text>
        </View>
    );
};

export default Home;
